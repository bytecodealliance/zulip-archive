[
    {
        "content": "<p>Byte-Naut requested <a href=\"https://github.com/pchickey\">pchickey</a> for a review on <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>.</p>",
        "id": 625465829,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1789850718
    },
    {
        "content": "<p>Byte-Naut requested <a href=\"https://github.com/orgs/bytecodealliance/teams/wasmtime-core-reviewers\">wasmtime-core-reviewers</a> for a review on <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>.</p>",
        "id": 625465830,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1789850718
    },
    {
        "content": "<p>Byte-Naut opened <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a> from <code>Byte-Naut:issue-14291-concurrent-resource-drop</code> to <code>bytecodealliance:main</code>:</p>\n<blockquote>\n<p>Closes #14291.</p>\n<h3>Motivation</h3>\n<p>With the component-model-async APIs there is currently no supported way to<br>\ndrop a guest-exported <code>ResourceAny</code> when the host only has an <code>Accessor</code>,<br>\ne.g. inside <code>Store::run_concurrent</code> or a spawned <code>AccessorTask</code>.<br>\n<code>resource_drop</code> and <code>resource_drop_async</code> both need a <code>StoreContextMut</code> that<br>\nlives across the destructor call, which cannot escape an <code>Accessor::with</code><br>\nclosure, and blocking inside the closure would stall the store's event loop.<br>\n@SilverMira ran into this in #9946 and @alexcrichton suggested a<br>\n<code>resource_drop_concurrent</code> method as the missing piece.</p>\n<h3>Changes</h3>\n<ul>\n<li>\n<p>Add <code>ResourceAny::resource_drop_concurrent(self, accessor: impl AsAccessor)</code><br>\n  behind <code>component-model-async</code>.</p>\n</li>\n<li>\n<p>Make <code>StoreOpaque::queue_task</code> <code>pub(crate)</code> so the new method can reuse the<br>\n  existing <code>WorkerFunction</code> path. No changes to the queue or event loop.</p>\n</li>\n<li>\n<p>Update the <code>resource_drop</code> docs to list all three variants and when each<br>\n  applies, which should also help with the documentation concerns raised in<br>\n  #9946.</p>\n</li>\n<li>\n<p>Tests: the <code>ApiStyle::Concurrent</code> arm of <code>thread_index_via_resource_drop</code><br>\n  now exercises the new method, and a new<br>\n<code>resource_drop_concurrent_from_accessor_task</code> test reproduces the<br>\n<code>AccessorTask</code> scenario from the issue.</p>\n</li>\n</ul>\n<h3>Design</h3>\n<p>The drop is queued as a <code>WorkerFunction</code> on the store's worker fiber using<br>\nthe same oneshot-plus-<code>StoreToken</code> pattern as <code>start_instance</code>. The<br>\n<code>Accessor::with</code> closure only creates the channel and enqueues the work, so<br>\nthe store borrow ends before the caller awaits the result. Running the<br>\ndestructor on the worker fiber keeps guest code off the embedder's stack.<br>\n<code>resource_drop_impl</code> is shared unchanged, so resource table handling,<br>\n<code>may_enter</code> checks, and sync-call bookkeeping are identical to the existing<br>\nsync and async variants.</p>\n<p>Two behaviours worth calling out for review:</p>\n<ul>\n<li>\n<p>Destructor errors are sent back to the caller over the channel rather than<br>\n  surfacing as an event loop error.</p>\n</li>\n<li>\n<p>Once the future has been polled and the drop is queued, dropping the future<br>\n  does not cancel the drop; the queued work still runs and the send failure<br>\n  is ignored.</p>\n</li>\n</ul>\n<p>Both are documented on the method. Happy to adjust either if a different<br>\npolicy is preferred.</p>\n<h3>Testing</h3>\n<p>The test component's destructor calls <code>thread.index</code> and traps if it is zero,<br>\nso the tests check that the destructor actually runs in a guest thread<br>\ncontext, not just that no error is returned.</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"n\">cargo</span><span class=\"w\"> </span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"o\">--</span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"n\">all</span><span class=\"w\"> </span><span class=\"n\">resource_drop</span>\n</code></pre></div>\n<p>Locally: fmt, clippy, <code>cargo doc --all-features</code> with <code>-D warnings</code>, and the<br>\n<code>component_model</code> test suite all pass.</p>\n<h3>Out of scope</h3>\n<p><code>bindgen</code>-generated resource bindings currently only expose <code>resource_drop</code><br>\nand <code>resource_drop_async</code>. Adding a <code>_concurrent</code> variant there seemed better<br>\nas a follow-up once the core API shape is settled, but I can fold it in here<br>\nif preferred.</p>\n<p>This PR was written with assistance from Claude (Anthropic); I reviewed and<br>\ntested the changes and take responsibility for them, per the Bytecode<br>\nAlliance AI Tool Use Policy.</p>\n</blockquote>",
        "id": 625465832,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1789850718
    },
    {
        "content": "<p>Byte-Naut edited <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>:</p>\n<blockquote>\n<p>Closes #14291.</p>\n<h3>Motivation</h3>\n<p>With the component-model-async APIs there is currently no supported way to drop a guest-exported <code>ResourceAny</code> when the host only has an <code>Accessor</code>, e.g. inside <code>Store::run_concurrent</code> or a spawned <code>AccessorTask</code>. <code>resource_drop</code> and <code>resource_drop_async</code> both need a <code>StoreContextMut</code> that lives across the destructor call, which cannot escape an <code>Accessor::with</code> closure, and blocking inside the closure would stall the store's event loop. @SilverMira ran into this in #9946 and @alexcrichton suggested a <code>resource_drop_concurrent</code> method as the missing piece.</p>\n<h3>Changes</h3>\n<ul>\n<li>Add <code>ResourceAny::resource_drop_concurrent(self, accessor: impl AsAccessor)</code> behind <code>component-model-async</code>.</li>\n<li>Make <code>StoreOpaque::queue_task</code> <code>pub(crate)</code> so the new method can reuse the existing <code>WorkerFunction</code> path. No changes to the queue or event loop.</li>\n<li>Update the <code>resource_drop</code> docs to list all three variants and when each applies, which should also help with the documentation concerns raised in #9946.</li>\n<li>Tests: the <code>ApiStyle::Concurrent</code> arm of <code>thread_index_via_resource_drop</code> now exercises the new method, and a new <code>resource_drop_concurrent_from_accessor_task</code> test reproduces the <code>AccessorTask</code> scenario from the issue.</li>\n</ul>\n<h3>Design</h3>\n<p>The drop is queued as a <code>WorkerFunction</code> on the store's worker fiber using the same oneshot-plus-<code>StoreToken</code> pattern as <code>start_instance</code>. The <code>Accessor::with</code> closure only creates the channel and enqueues the work, so the store borrow ends before the caller awaits the result. Running the destructor on the worker fiber keeps guest code off the embedder's stack. <code>resource_drop_impl</code> is shared unchanged, so resource table handling, <code>may_enter</code> checks, and sync-call bookkeeping are identical to the existing sync and async variants.</p>\n<p>Two behaviours worth calling out for review:</p>\n<ul>\n<li>Destructor errors are sent back to the caller over the channel rather than surfacing as an event loop error.</li>\n<li>Once the future has been polled and the drop is queued, dropping the future does not cancel the drop; the queued work still runs and the send failure is ignored.</li>\n</ul>\n<p>Both are documented on the method. Happy to adjust either if a different policy is preferred.</p>\n<h3>Testing</h3>\n<p>The test component's destructor calls <code>thread.index</code> and traps if it is zero, so the tests check that the destructor actually runs in a guest thread context, not just that no error is returned.</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"n\">cargo</span><span class=\"w\"> </span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"o\">--</span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"n\">all</span><span class=\"w\"> </span><span class=\"n\">resource_drop</span>\n</code></pre></div>\n<p>Locally: fmt, clippy, <code>cargo doc --all-features</code> with <code>-D warnings</code>, and the <code>component_model</code> test suite all pass.</p>\n<h3>Out of scope</h3>\n<p><code>bindgen</code>-generated resource bindings currently only expose <code>resource_drop</code> and <code>resource_drop_async</code>. Adding a <code>_concurrent</code> variant there seemed better as a follow-up once the core API shape is settled, but I can fold it in here if preferred.</p>\n<p>This PR was written with assistance from Claude (Anthropic); I reviewed and tested the changes and take responsibility for them, per the Bytecode Alliance AI Tool Use Policy.</p>\n</blockquote>",
        "id": 625466151,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1789850995
    },
    {
        "content": "<p>Byte-Naut edited <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>:</p>\n<blockquote>\n<p>Closes #14291.</p>\n<h3>Motivation</h3>\n<p>With the component-model-async APIs there is currently no supported way to drop a guest-exported <code>ResourceAny</code> when the host only has an <code>Accessor</code>, e.g. inside <code>Store::run_concurrent</code> or a spawned <code>AccessorTask</code>. <code>resource_drop</code> and <code>resource_drop_async</code> both need a <code>StoreContextMut</code> that lives across the destructor call, which cannot escape an <code>Accessor::with</code> closure, and blocking inside the closure would stall the store's event loop. @SilverMira ran into this in #9946 and @alexcrichton suggested a <code>resource_drop_concurrent</code> method as the missing piece.</p>\n<h3>Changes</h3>\n<ul>\n<li>Add <code>ResourceAny::resource_drop_concurrent(self, accessor: impl AsAccessor)</code> behind <code>component-model-async</code>.</li>\n<li>Make <code>StoreOpaque::queue_task</code> <code>pub(crate)</code> so the new method can reuse the existing <code>WorkerFunction</code> path. No changes to the queue or event loop.</li>\n<li>Update the <code>resource_drop</code> docs to list all three variants and when each applies, which should also help with the documentation concerns raised in #9946.</li>\n<li>Tests: the <code>ApiStyle::Concurrent</code> arm of <code>thread_index_via_resource_drop</code> now exercises the new method, and a new <code>resource_drop_concurrent_from_accessor_task</code> test reproduces the <code>AccessorTask</code> scenario from the issue.</li>\n</ul>\n<h3>Design</h3>\n<p>The drop is queued as a <code>WorkerFunction</code> on the store's worker fiber using the same oneshot-plus-<code>StoreToken</code> pattern as <code>start_instance</code>. The <code>Accessor::with</code> closure only creates the channel and enqueues the work, so the store borrow ends before the caller awaits the result. Running the destructor on the worker fiber keeps guest code off the embedder's stack. <code>resource_drop_impl</code> is shared unchanged, so resource table handling, <code>may_enter</code> checks, and sync-call bookkeeping are identical to the existing sync and async variants.</p>\n<p>Two behaviours worth calling out for review:</p>\n<ul>\n<li>Destructor errors are sent back to the caller over the channel rather than surfacing as an event loop error.</li>\n<li>Once the future has been polled and the drop is queued, dropping the future does not cancel the drop; the queued work still runs and the send failure is ignored.</li>\n</ul>\n<p>Both are documented on the method. Happy to adjust either if a different policy is preferred.</p>\n<h3>Testing</h3>\n<p>The test component's destructor calls <code>thread.index</code> and traps if it is zero, so the tests check that the destructor actually runs in a guest thread context, not just that no error is returned.</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"n\">cargo</span><span class=\"w\"> </span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"o\">--</span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"n\">all</span><span class=\"w\"> </span><span class=\"n\">resource_drop</span>\n</code></pre></div>\n<p>Locally: fmt, clippy, <code>cargo doc --all-features</code> with <code>-D warnings</code>, and the <code>component_model</code> test suite all pass.</p>\n<h3>Out of scope</h3>\n<p><code>bindgen</code>-generated resource bindings currently only expose <code>resource_drop</code> and <code>resource_drop_async</code>. Adding a <code>_concurrent</code> variant there seemed better as a follow-up once the core API shape is settled, but I can fold it in here if preferred.</p>\n</blockquote>",
        "id": 625466922,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1789851680
    },
    {
        "content": "<p>Byte-Naut edited <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>:</p>\n<blockquote>\n<p>Closes #14291.</p>\n<h3>Motivation</h3>\n<p>With the component-model-async APIs there is currently no supported way to drop a guest-exported <code>ResourceAny</code> when the host only has an <code>Accessor</code>, e.g. inside <code>Store::run_concurrent</code> or a spawned <code>AccessorTask</code>. <code>resource_drop</code> and <code>resource_drop_async</code> both need a <code>StoreContextMut</code> that lives across the destructor call, which cannot escape an <code>Accessor::with</code> closure, and blocking inside the closure would stall the store's event loop. @SilverMira ran into this in #9946 and @alexcrichton suggested a <code>resource_drop_concurrent</code> method as the missing piece.</p>\n<h3>Changes</h3>\n<ul>\n<li>Add <code>ResourceAny::resource_drop_concurrent(self, accessor: impl AsAccessor)</code> behind <code>component-model-async</code>.</li>\n<li>Make <code>StoreOpaque::queue_task</code> <code>pub(crate)</code> so the new method can reuse the existing <code>WorkerFunction</code> path. No changes to the queue or event loop.</li>\n<li>Update the <code>resource_drop</code> docs to list all three variants and when each applies, which should also help with the documentation concerns raised in #9946.</li>\n<li>Tests: the <code>ApiStyle::Concurrent</code> arm of <code>thread_index_via_resource_drop</code> now exercises the new method, and a new <code>resource_drop_concurrent_from_accessor_task</code> test reproduces the <code>AccessorTask</code> scenario from the issue.</li>\n</ul>\n<h3>Design</h3>\n<p>The drop is queued as a <code>WorkerFunction</code> on the store's worker fiber using the same oneshot-plus-<code>StoreToken</code> pattern as <code>start_instance</code>. The <code>Accessor::with</code> closure only creates the channel and enqueues the work, so the store borrow ends before the caller awaits the result. Running the destructor on the worker fiber keeps guest code off the embedder's stack. <code>resource_drop_impl</code> is shared unchanged, so resource table handling, <code>may_enter</code> checks, and sync-call bookkeeping are identical to the existing sync and async variants.</p>\n<p>Two behaviours worth calling out for review:</p>\n<ul>\n<li>Destructor errors are sent back to the caller over the channel rather than surfacing as an event loop error.</li>\n<li>Once the future has been polled and the drop is queued, dropping the future does not cancel the drop; the queued work still runs and the send failure is ignored.</li>\n</ul>\n<p>Both are documented on the method. Happy to adjust either if a different policy is preferred.</p>\n<h3>Testing</h3>\n<p>The test component's destructor calls <code>thread.index</code> and traps if it is zero, so the tests check that the destructor actually runs in a guest thread context, not just that no error is returned.</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"n\">cargo</span><span class=\"w\"> </span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"o\">--</span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"n\">all</span><span class=\"w\"> </span><span class=\"n\">resource_drop</span>\n</code></pre></div>\n<p>Locally: fmt, clippy, <code>cargo doc --all-features</code> with <code>-D warnings</code>, and the <code>component_model</code> test suite all pass.</p>\n<h3>Out of scope</h3>\n<p><code>bindgen</code>-generated resource bindings currently only expose <code>resource_drop</code> and <code>resource_drop_async</code>. Adding a <code>_concurrent</code> variant there seemed better as a follow-up once the core API shape is settled, but I can fold it in here if preferred.</p>\n<p>Developed with assistance from Claude (Anthropic). Per the Bytecode Alliance AI Tool Use Policy, I'm the author and accountable for this change.</p>\n</blockquote>",
        "id": 625467340,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1789852007
    },
    {
        "content": "<p>github-actions[bot] added the label <code>wasmtime:api</code> on <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>.</p>",
        "id": 625475231,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1789857863
    },
    {
        "content": "<p>alexcrichton unassigned pchickey from <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362 component: add <code>ResourceAny::resource_drop_concurrent</code></a>.</p>",
        "id": 625810445,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790012010
    },
    {
        "content": "<p>alexcrichton requested <a href=\"https://github.com/dicej\">dicej</a> for a review on <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>.</p>",
        "id": 625810446,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790012010
    },
    {
        "content": "<p><span aria-label=\"thumbs up\" class=\"emoji emoji-1f44d\" role=\"img\" title=\"thumbs up\">:thumbs_up:</span> dicej submitted <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362#pullrequestreview-5272500080\">PR review</a>:</p>\n<blockquote>\n<p>LGTM, thanks; just one suggestion inline to simplify the test.</p>\n</blockquote>",
        "id": 625870435,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790030268
    },
    {
        "content": "<p><span aria-label=\"speech balloon\" class=\"emoji emoji-1f4ac\" role=\"img\" title=\"speech balloon\">:speech_balloon:</span> dicej created <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362#discussion_r4066995947\">PR review comment</a>:</p>\n<blockquote>\n<p>The use of <code>AccessorTask</code>, <code>Store::spawn</code> and the <code>oneshot::channel</code> seems unnecessary here.  Instead, I'd suggest moving lines 3296 and 3297 into the body of the <code>AsyncFn</code> passed to <code>Store::run_concurrent</code>, which would simplify this test a lot while still exercising the new code path.</p>\n</blockquote>",
        "id": 625870436,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790030268
    },
    {
        "content": "<p>Byte-Naut updated <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>.</p>",
        "id": 625900986,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790043258
    },
    {
        "content": "<p>Byte-Naut edited <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>:</p>\n<blockquote>\n<p>Closes #14291.</p>\n<h3>Motivation</h3>\n<p>With the component-model-async APIs there is currently no supported way to drop a guest-exported <code>ResourceAny</code> when the host only has an <code>Accessor</code>, e.g. inside <code>Store::run_concurrent</code> or a spawned <code>AccessorTask</code>. <code>resource_drop</code> and <code>resource_drop_async</code> both need a <code>StoreContextMut</code> that lives across the destructor call, which cannot escape an <code>Accessor::with</code> closure, and blocking inside the closure would stall the store's event loop. @SilverMira ran into this in #9946 and @alexcrichton suggested a <code>resource_drop_concurrent</code> method as the missing piece.</p>\n<h3>Changes</h3>\n<ul>\n<li>Add <code>ResourceAny::resource_drop_concurrent(self, accessor: impl AsAccessor)</code> behind <code>component-model-async</code>.</li>\n<li>Make <code>StoreOpaque::queue_task</code> <code>pub(crate)</code> so the new method can reuse the existing <code>WorkerFunction</code> path. No changes to the queue or event loop.</li>\n<li>Update the <code>resource_drop</code> docs to list all three variants and when each applies, which should also help with the documentation concerns raised in #9946.</li>\n<li>Tests: the <code>ApiStyle::Concurrent</code> arm of <code>thread_index_via_resource_drop</code> now exercises the new method, and a new <code>resource_drop_concurrent_in_run_concurrent</code> test calls the guest and drops the returned resource inside a single <code>run_concurrent</code>, the scenario from the issue.</li>\n</ul>\n<h3>Design</h3>\n<p>The drop is queued as a <code>WorkerFunction</code> on the store's worker fiber using the same oneshot-plus-<code>StoreToken</code> pattern as <code>start_instance</code>. The <code>Accessor::with</code> closure only creates the channel and enqueues the work, so the store borrow ends before the caller awaits the result. Running the destructor on the worker fiber keeps guest code off the embedder's stack. <code>resource_drop_impl</code> is shared unchanged, so resource table handling, <code>may_enter</code> checks, and sync-call bookkeeping are identical to the existing sync and async variants.</p>\n<p>Two behaviours worth calling out for review:</p>\n<ul>\n<li>Destructor errors are sent back to the caller over the channel rather than surfacing as an event loop error.</li>\n<li>Once the future has been polled and the drop is queued, dropping the future does not cancel the drop; the queued work still runs and the send failure is ignored.</li>\n</ul>\n<p>Both are documented on the method. Happy to adjust either if a different policy is preferred.</p>\n<h3>Testing</h3>\n<p>The test component's destructor calls <code>thread.index</code> and traps if it is zero, so the tests check that the destructor actually runs in a guest thread context, not just that no error is returned.</p>\n<div class=\"codehilite\" data-code-language=\"Rust\"><pre><span></span><code><span class=\"n\">cargo</span><span class=\"w\"> </span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"o\">--</span><span class=\"n\">test</span><span class=\"w\"> </span><span class=\"n\">all</span><span class=\"w\"> </span><span class=\"n\">resource_drop</span>\n</code></pre></div>\n<p>Locally: fmt, clippy, <code>cargo doc --all-features</code> with <code>-D warnings</code>, and the <code>component_model</code> test suite all pass.</p>\n<h3>Out of scope</h3>\n<p><code>bindgen</code>-generated resource bindings currently only expose <code>resource_drop</code> and <code>resource_drop_async</code>. Adding a <code>_concurrent</code> variant there seemed better as a follow-up once the core API shape is settled, but I can fold it in here if preferred.</p>\n<p>Developed with assistance from Claude (Anthropic). Per the Bytecode Alliance AI Tool Use Policy, I'm the author and accountable for this change.</p>\n</blockquote>",
        "id": 625901178,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790043335
    },
    {
        "content": "<p><span aria-label=\"memo\" class=\"emoji emoji-1f4dd\" role=\"img\" title=\"memo\">:memo:</span> Byte-Naut submitted <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362#pullrequestreview-5273588364\">PR review</a>.</p>",
        "id": 625901280,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790043392
    },
    {
        "content": "<p><span aria-label=\"speech balloon\" class=\"emoji emoji-1f4ac\" role=\"img\" title=\"speech balloon\">:speech_balloon:</span> Byte-Naut created <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362#discussion_r4067951067\">PR review comment</a>:</p>\n<blockquote>\n<p>Good call, thanks. Simplified the test to run the call and drop inside a single run_concurrent closure and dropped the AccessorTask scaffolding; renamed it to resource_drop_concurrent_in_run_concurrent to match.</p>\n</blockquote>",
        "id": 625901281,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790043392
    },
    {
        "content": "<p>Byte-Naut requested <a href=\"https://github.com/dicej\">dicej</a> for a review on <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>.</p>",
        "id": 625901381,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790043429
    },
    {
        "content": "<p>dicej added <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362 component: add <code>ResourceAny::resource_drop_concurrent</code></a> to the merge queue</p>",
        "id": 626023894,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790082880
    },
    {
        "content": "<p><span aria-label=\"check\" class=\"emoji emoji-2705\" role=\"img\" title=\"check\">:check:</span> dicej merged <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362</a>.</p>",
        "id": 626031620,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790084531
    },
    {
        "content": "<p>dicej removed <a href=\"https://github.com/bytecodealliance/wasmtime/pull/14362\">PR #14362 component: add <code>ResourceAny::resource_drop_concurrent</code></a> from the merge queue</p>",
        "id": 626031623,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1790084532
    }
]